import React from 'react';
import AppBar from 'material-ui/AppBar';
import logo from '../logo.svg';

const Header = (props) => {
  return (
    <div>
      <AppBar title="Foodie Diary"/>
    </div>
  );
}

export default Header;